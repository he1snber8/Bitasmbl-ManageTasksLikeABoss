// client/src/components/TaskList.js\nimport React, { useEffect } from "react";\nimport { useSelector, useDispatch } from "react-redux";\nimport { fetchTasks } from "../actions/taskActions";\nconst TaskList = () => { const dispatch = useDispatch(); const tasks = useSelector(s=>s.tasks); useEffect(()=>dispatch(fetchTasks()),[dispatch]); return (<ul>{tasks.sort((a,b)=>b.priority-a.priority).map(t=><li key={t._id}>{t.title}</li>)}</ul>);} ;\nexport default TaskList;
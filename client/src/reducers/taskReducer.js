// client/src/reducers/taskReducer.js\nconst initialState = [];\nexport default function taskReducer(state=initialState, action) { switch(action.type) { case "SET_TASKS": return action.payload; default: return state; } }